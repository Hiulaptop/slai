## 1. Domain and Bootstrap Backend

- [x] 1.1 Add design bootstrap request schema (title, mode blank|template, optional templateId, optional blank slideCount) and design-save schema (generationId, html, expectedRevision) — `modules/slides/domain/slide.schemas.ts` (`designBootstrapSchema`, `designSaveSchema`)
- [x] 1.2 Implement blank HTML document builder with contiguous `.slai-slide` wrappers, default 16:9 CSS, and no external resources — `modules/slides/domain/html.ts` (`buildBlankPresentationHtml`), reuses `validatePresentationHtml`'s existing sanitize/validate pipeline rather than a parallel one
- [~] 1.3 Implement template-seeded HTML builder that resolves owned templates, applies skeleton/assets, embeds assets server-side, and sanitizes — **not implemented; genuine gap, not a fabrication.** There is no template library in this codebase at all: no `Template` Prisma model, no template storage, no template list/read API. `proposal.md` lists `user-template-library` as an existing "Modified Capability," but that's inaccurate against the current schema (`prisma/schema.prisma` has no template-related model whatsoever) — same class of doc/reality mismatch as `Docs/DESIGN.md`'s stale claims found earlier this session. `SlideService.bootstrapDesign` accepts `mode: "template"` at the schema level (forward-compatible) but throws `SlideError("INVALID_INPUT", "Template-based design projects are not available yet")` rather than faking template resolution against nonexistent data. Building a real template library (data model + migration + storage + ownership + asset embedding) is a separate, unscoped effort — worth its own OpenSpec change, not something to improvise inside this one.
- [x] 1.4 Add slide service methods for bootstrap and design save with owner policy, sanitization, size limits, and revision CAS — `modules/slides/application/slide.service.ts` (`bootstrapDesign`, `saveDesign`); `saveDesign` uses `PresentationAccessPolicy`'s existing `"mutate"` operation (already enforces COMPLETED-status + ownership, no policy changes needed) and CAS's on the *client-supplied* `expectedRevision` (not just a same-request re-fetch), so a long-stale tab correctly gets `409` too
- [x] 1.5 Expose authenticated `POST` bootstrap and design-save routes; extend auth policy list to include the new operations — `app/api/slides/design/bootstrap/route.ts` (`POST`), `app/api/slides/design/save/route.ts` (`PATCH`); no `PresentationAccessPolicy` changes needed (see 1.4)
- [x] 1.6 Add domain/service/route tests for blank/template bootstrap, ownership denial, invalid input, unsafe HTML rejection, and concurrent save `409` — `modules/slides/domain/slides.test.ts`, `modules/slides/application/slide.service.test.ts`, `modules/slides/infrastructure/prisma-slide.repository.test.ts`, `app/api/slides/slide-routes.test.ts`

Revision operation label: reused the existing `EDIT` enum value for design saves rather than adding a new `DESIGN` value to `SlideRevisionOperation` (would need a Prisma migration) — this was `design.md`'s own stated fallback under "Open Questions" when analytics separation isn't required.

Endpoints implemented exactly as called by the frontend (`components/design-project-setup.tsx`, `components/design-editor.tsx`):
- `POST /api/slides/design/bootstrap` — body `{ title, mode: "blank"|"template", templateId?, slideCount? }` → `presentationResponse` shape, `201`. `mode: "template"` currently always `400`s (see 1.3).
- `PATCH /api/slides/design/save` — body `{ generationId, html, expectedRevision }` → `presentationResponse` shape, `200`/`409` on stale revision.
- `GET /api/templates` — **still not implemented**, needed for task 2.2's template picker once 1.3's template library exists.

## 2. Create Path Chooser and Design Setup UI

- [x] 2.1 Replace `/slides/new` entry with a path chooser: design-from-template-or-blank vs generate-from-report — `components/slide-creation-entry.tsx`
- [x] 2.2 Implement design project setup form (title, blank vs template, owned template list, blank slide count) with field validation — `components/design-project-setup.tsx`
- [x] 2.3 Wire bootstrap API call, pending/error/retry, and navigation to the design editor on success — `components/design-project-setup.tsx`
- [x] 2.4 Preserve the existing report creation workspace behind the generate-from-report choice — `components/slide-creation-workspace.tsx` unchanged, now rendered by `SlideCreationEntry`
- [x] 2.5 Add UI tests for chooser, blank/template setup, validation, unauthorized redirect, and successful editor navigation — `components/slide-creation-entry.test.tsx`, `components/design-project-setup.test.tsx`, `components/slide-creation-workspace.test.tsx` (moved from the old page test). Unauthorized redirect is already covered at the layout level by `components/protected-shell.tsx` (untouched), which every route under `app/(protected)/**` inherits for free.

## 3. Visual Design Editor Canvas and Tools

- [x] 3.1 Add protected design editor route/mode for `/slides/[generationId]` design surface with detail loading and not-found/error states — `app/(protected)/slides/[generationId]/design/page.tsx` as a sibling route
- [x] 3.2 Implement canvas that loads one slide at a time from sanitized HTML under the wrapper contract — `lib/slides/design-document.ts` (`parseDesignSlides`) + `components/design-canvas.tsx`
- [x] 3.3 Implement tools: select/move/resize, text, rectangle, ellipse, line, image place, layer order, delete element — `components/design-canvas.tsx`, `components/design-editor.tsx`
- [x] 3.4 Enforce client allowlists (no scripts/handlers/forms/remote URLs) and slide-boundary clipping — elements are only ever constructed as typed objects (never raw HTML), `parseDesignSlides` drops unrecognized element types and non-`data:` image sources, `clampToSlide` bounds every element on create/move/resize/parse; server re-sanitizes via `validatePresentationHtml` on every save (task 1.4) as the actual source of truth
- [x] 3.5 Implement slide strip: add, delete (min one), reorder with contiguous renumbering — `components/design-editor.tsx`, `renumberSlides` in `lib/slides/design-document.ts`
- [x] 3.6 Implement local undo stack for unsaved tool actions and dirty-state tracking — `components/design-editor.tsx` (`history`/`applyMutation`/`dirty`)
- [x] 3.7 Add editor UI tests for tools, safety blocks, slide management, and load states — `components/design-canvas.test.tsx`, `components/design-editor.test.tsx`

## 4. Save, Export, and Library Integration

- [x] 4.1 Implement Save that serializes canvas → full HTML, sends expectedRevision, applies response HTML/revision, clears dirty state — `components/design-editor.tsx` (`save`), backed by `PATCH /api/slides/design/save` (task 1.4/1.5)
- [x] 4.2 Handle save conflicts (`409`), validation errors, and network failures without dropping local drafts incorrectly — `components/design-editor.tsx` (`save`); server-side CAS on client `expectedRevision` (task 1.4) is what actually produces the `409`
- [x] 4.3 Integrate Download HTML with `exportStandalonePresentation`; prompt or auto-save when dirty — `components/design-editor.tsx` (`download`): silent save attempt when dirty, falls back to exporting the current local canvas if that save fails
- [~] 4.4 Route design-origin (and design-editable) library cards to the design editor — **partially done**: manual "Open in design editor" link added to every dashboard card (`components/presentation-dashboard.tsx`); automatic origin-based routing still needs a genuine backend field (no `origin`/`mode` discriminator on `PresentationSummary`/`StoredPresentation` — confirmed directly against `modules/slides/application/slide.ports.ts` and `prisma/schema.prisma`, not assumed). `bootstrapDesign` sets `provider: "design"`/`modelId: "blank"` (vs real AI provider names for generated decks) which *could* become that discriminator on the list endpoint if a future change threads it through `PresentationSummary` — noted for whoever picks this up, not implemented here since `PresentationSummary` intentionally excludes `provider` today (see `modules/slides/presentation/route-helpers.ts`'s `presentationSummaryResponse`).
- [x] 4.5 Add tests for save/export flows, dirty download guard, and dashboard navigation — `components/design-editor.test.tsx`, `components/presentation-dashboard.test.tsx`

## 5. Verification

- [x] 5.1 Run lint, typecheck, unit/API/UI tests, OpenSpec validate, and production build — `pnpm lint`, `pnpm typecheck`, `pnpm test` (206/206), `openspec validate add-visual-slide-design-editor --type change` (valid), `pnpm build` all pass clean, including both new routes (`/api/slides/design/bootstrap`, `/api/slides/design/save`) showing in the build's route table. Required generating the Prisma client and adding the missing `@types/sanitize-html` devDependency (both pre-existing environment/dependency gaps unrelated to this change's code, blocking a clean baseline build).
- [~] 5.2 Browser-verify: Create → blank → tools → save → download HTML; Create → template → design → export; ownership isolation; offline nav of exported file — **partially verified**: `pnpm dev` starts cleanly and serves `/`. Full logged-in browser click-through (register → create → blank → tools → save → download) was not exercised because this environment has no reachable MySQL (`DATABASE_URL` points to a local instance that isn't running here) — the same limitation applies to every other route in this codebase, none of which have live-DB end-to-end tests either, only mocked unit/integration tests (which this change matches). Template path is blocked entirely on 1.3.
